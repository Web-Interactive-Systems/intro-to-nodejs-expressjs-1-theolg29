const http = require("http");

/**
 Todo Complete this app to serve the content inside `static` folder
 
 - The app should show an app navigation, see index.html
 - If an url is not found the app should serve "Page not found"
 */

//  for example, we can read files using `readFileSync` of fs module

const server = http.createServer((req, res) => {
  //
});

server.listen(3000);
